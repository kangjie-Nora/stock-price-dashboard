# 🚀 项目启动说明

## 快速启动

在项目目录中运行以下命令：

```bash
# 1. 进入项目目录
cd "/Users/lukangjie/Desktop/Stock Price Dashboard"

# 2. 确保 nvm 已加载（如果还没加载）
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
nvm use --lts

# 3. 启动开发服务器
npm run dev
```

或者，如果您已经打开了新的终端窗口（nvm 会自动加载），直接运行：

```bash
cd "/Users/lukangjie/Desktop/Stock Price Dashboard"
npm run dev
```

## 访问应用

启动成功后，终端会显示类似以下信息：

```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
  ➜  press h + enter to show help
```

在浏览器中打开：**http://localhost:5173**

## 停止服务器

在运行服务器的终端窗口中按 `Ctrl + C` 即可停止。

## 其他常用命令

```bash
# 构建生产版本
npm run build

# 预览生产构建
npm run preview

# 检查代码格式
npm run lint
```

## 注意事项

- 首次运行 `npm install` 已安装所有依赖
- 如果修改了代码，Vite 会自动热更新，无需重启
- 如果遇到端口占用，Vite 会自动使用下一个可用端口

祝您使用愉快！🎉

